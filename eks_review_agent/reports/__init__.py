"""Reports — search into saved reports and JIRA CSV export.

report_search.py provides BM25-style keyword search across saved
markdown reports for the @tool exposed to the agent. export.py
parses saved reports deterministically into JIRA-importable CSV.
"""
