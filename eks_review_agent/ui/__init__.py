"""UI primitives — terminal picker and logging configuration.

These are infrastructure used by the CLI but kept separate so the
cli/ package can stay focused on slash command handlers and REPL
flow.
"""
